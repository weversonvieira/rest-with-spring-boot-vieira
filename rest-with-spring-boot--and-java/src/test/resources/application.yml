security:
  jwt:
    token:
      secret-key: 53cr37
      expire-lenght: 3600000

spring:
  application:
    name:rest_with_spring_boot

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot?useTimezone=true&serverTimezone=UTC
    username: root
    password: V1e1r@2022

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false

  logging:
    level:
      root: WARN

  spring-doc:
    paths-to-match: /api/**/v1/**
    swagger-ui:
      use-root-path: true



